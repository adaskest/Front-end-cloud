import React, {useRef, useState} from 'react';import {Box, Button, TextField, Typography} from "@mui/material";import http from "../plugins/http";const Register = ({setShowComponent}) => {    const user = useRef()    const passOne = useRef()    const passTwo = useRef()    const [error, setError] = useState('')    async function register() {        const newUser = {            user: user.current.value,            passwordOne: passOne.current.value,            passwordTwo: passTwo.current.value        }        const data = await http.post(newUser, 'register')        await (data.success) ? setShowComponent(false) : setError(data.message)    }    return (        <Box width={400}>            <TextField                margin='normal'                placeholder='5 to 12 characters'                label="Username"                fullWidth                inputRef={user}            />            <TextField                margin='normal'                placeholder='5 to 20 characters, including at least one number'                label="Password One"                type="password"                fullWidth                inputRef={passOne}            />            <TextField                margin='normal'                placeholder='5 to 20 characters, including at least one number'                label="Password Two"                type="password"                fullWidth                inputRef={passTwo}            />            <div style={{display: "flex", justifyContent: "center"}}>                <Button onClick={register} color='error' variant='contained'>register</Button>            </div>            {error && <Typography variant={"subtitle1"}                                  style={{                                      backgroundColor: "#ff8500",                                      fontSize: '18px',                                      padding: 5,                                      borderRadius: 10,                                      textAlign: "center",                                      marginTop: 30                                  }}>                {error}            </Typography>}        </Box>    );};export default Register;