import React, {useContext, useEffect, useState} from 'react';import {AppBar, Avatar, CardMedia, Toolbar, Typography} from "@mui/material";import {useNavigate} from 'react-router-dom'import context from "../context/context";import {ImExit} from "react-icons/im";import http from "../plugins/http";import logo from '../logo/PoweredBy_200_Horizontal_Light-Backgrounds_With_Logo.gif'import {deepOrange} from "@mui/material/colors";const Header = () => {    const nav = useNavigate()    const {user, setUser} = useContext(context)    const [showUsername, setShowUsername] = useState(false)    console.log(user)    useEffect(() => {        if (!user) nav('/authentication')    }, [])    async function exit() {        await http.get('exit')        setUser('')        nav('/authentication')    }    function username() {        setShowUsername(!showUsername)    }    return (        <AppBar position={"static"}>            <Toolbar style={{backgroundColor: '#22223b', justifyContent: "space-between", fontSize: 20}}>                <div>                    <CardMedia                        component="img"                        height="100%"                        image={logo}                        alt="gif"                    />                </div>                {user ? <div style={{color: "white", display: "flex", alignItems: "center"}}>                    <Avatar sx={{bgcolor: deepOrange[500], mr: 2}} style={{position: "relative"}}                            onMouseEnter={username} onMouseLeave={username}>{user[0].toUpperCase()}</Avatar>                    {showUsername && <div style={{position: "absolute", right: '100px', top: '20px'}}>                        <Typography variant='button' style={{marginRight: 20, fontSize: 16}}>                            {user}                        </Typography>                    </div>}                    <ImExit onClick={exit} style={{cursor: "pointer"}}/>                </div> : <div/>}            </Toolbar>        </AppBar>    );};export default Header;